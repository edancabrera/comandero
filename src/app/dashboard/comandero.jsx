import { StyleSheet, Text, View } from 'react-native'

const Comandero = () => {
  return (
    <View>
      <Text>Pantalla Comandero</Text>
    </View>
  )
}

export default Comandero

const styles = StyleSheet.create({})